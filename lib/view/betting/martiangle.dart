import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Martiangle extends StatelessWidget {
  const Martiangle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
