import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Labouchere extends StatelessWidget {
  const Labouchere({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
